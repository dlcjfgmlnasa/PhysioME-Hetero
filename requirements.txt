# PhysioME-Hetero — direct project dependencies
#
# Pin only where we hit known compatibility issues. For PyTorch / CUDA wheels
# refer to https://pytorch.org/get-started/locally/ — the right command depends
# on the user's CUDA toolkit version.

# Core scientific stack
numpy>=1.26
scipy>=1.13

# Deep learning
torch>=2.1
torchvision>=0.16
timm>=1.0
einops>=0.8

# LoRA: hand-rolled in models/transformer/lora.py (no peft / transformers dep).

# Self-supervised / metrics / preprocessing
scikit-learn>=1.3
mne>=1.6
tensorboard>=2.14

# Biosignal datasets
vitaldb>=1.6                 # VitalDB raw .vital file API
wfdb>=4.0                    # MIMIC-III Waveform DB streaming

# Utilities
PyYAML>=6.0
tqdm>=4.65
matplotlib>=3.8
